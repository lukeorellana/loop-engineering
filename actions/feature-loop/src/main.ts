import * as core from '@actions/core';

/**
 * Runs the Feature Loop action.
 *
 * This is a placeholder entry point for the Feature Loop GitHub Action. No
 * loop behavior is implemented yet; it simply reports that Feature Loop is not
 * yet implemented and exits successfully.
 */
export async function run(): Promise<void> {
  core.info('Feature Loop is not yet implemented.');
}
